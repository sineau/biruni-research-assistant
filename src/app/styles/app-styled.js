import styled from 'styled-components'

const black = '#efefef'

const AppStyled = styled.div`
  display: flex
`
const TreeStyled = styled.div`
  flex: 1 0 15%;
`
const MainStyled = styled.div`
  flex: 1 0 85%;
  padding-left: 10px;
`
export { AppStyled, TreeStyled, MainStyled }
